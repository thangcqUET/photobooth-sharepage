# Development runtime environment for the sharepage
# Allow any media host during local development for convenience.
# WARNING: Do NOT use '*' in production.
VITE_ALLOWED_MEDIA_HOSTS=*
# VITE_ALLOWED_MEDIA_HOSTS=xxx.example.com,yyy.example.com

# When true, user must follow one of the social channels before download/share is enabled.
# Set to false for local development convenience.
VITE_REQUIRE_FOLLOW=true
